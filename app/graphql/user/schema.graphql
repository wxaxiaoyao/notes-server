

type User {
	id: Int
	username: String
	nickname: String
	email: String
	cellphone: String
	portrait: String
	sex: String
	description: String
	extra: Json
	statistics: Statistics
}

type Statistics {
	documentCount: Int
	noteCount: Int
	dailyCount: Int
	fansCount: Int
	followCount: Int
}
